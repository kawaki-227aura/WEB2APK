WEB2APK KAWAKI227 V3 — 1-CLICK DEPLOY

Le dépôt contient déjà render.yaml et Dockerfile.
Sur Render: New + -> Blueprint -> choisir ce dépôt -> Apply.
Render lit render.yaml et crée le Web Service + disque persistant.

Après le premier déploiement, ouvrir l'URL Render.
